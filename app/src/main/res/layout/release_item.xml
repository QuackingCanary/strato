<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <!-- Release header row (clickable) -->
    <LinearLayout
        android:id="@+id/release_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/selectableItemBackground"
        android:clickable="true"
        android:focusable="true"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingHorizontal="16dp"
        android:paddingVertical="1dp">

        <com.google.android.material.chip.Chip
            android:id="@+id/release_tag_chip"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:clickable="false"
            android:focusable="false"
            android:textSize="10sp"
            app:chipMinHeight="20dp"
            tools:text="v24.3.1" />

        <ImageView
            android:id="@+id/release_installed_indicator"
            android:layout_width="14dp"
            android:layout_height="14dp"
            android:layout_marginStart="6dp"
            android:contentDescription="@null"
            android:visibility="gone"
            app:srcCompat="@drawable/ic_check"
            app:tint="?attr/colorPrimary"
            tools:visibility="gone" />

        <TextView
            android:id="@+id/release_date"
            style="?attr/textAppearanceBodySmall"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="10dp"
            android:textColor="?android:attr/textColorSecondary"
            tools:text="15 Jan 2025" />

        <ImageView
            android:id="@+id/release_expand_arrow"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:clickable="false"
            android:contentDescription="@null"
            android:focusable="false"
            app:srcCompat="@drawable/ic_keyboard_arrow_down"
            app:tint="?android:attr/textColorSecondary" />

    </LinearLayout>

    <!-- Expandable release content -->
    <LinearLayout
        android:id="@+id/release_expandable"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingHorizontal="12dp"
        android:paddingBottom="6dp"
        android:visibility="gone"
        tools:visibility="visible">

        <TextView
            android:id="@+id/release_changelog"
            style="?attr/textAppearanceBodyMedium"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="6dp"
            android:ellipsize="end"
            android:maxLines="6"
            android:textColor="?android:attr/textColorSecondary"
            android:visibility="gone"
            tools:text="## Changes\n- Fixed rendering artifacts on Adreno 7xx\n- Improved performance"
            tools:visibility="visible" />

        <TextView
            android:id="@+id/see_more_button"
            style="?attr/textAppearanceLabelMedium"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:layout_marginBottom="6dp"
            android:clickable="true"
            android:focusable="true"
            android:text="@string/see_more"
            android:textColor="?attr/colorPrimary"
            android:visibility="gone" />

        <LinearLayout
            android:id="@+id/release_download_buttons"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />

        <com.google.android.material.progressindicator.LinearProgressIndicator
            android:id="@+id/release_download_progress"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:visibility="gone"
            app:trackCornerRadius="4dp" />

        <TextView
            android:id="@+id/release_download_status"
            style="?attr/textAppearanceBodySmall"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:gravity="center"
            android:visibility="gone"
            tools:text="Driver installed successfully"
            tools:visibility="gone" />

    </LinearLayout>

</LinearLayout>
